---
import BaseLayout from '../layouts/BaseLayout.astro';

/* Components */
import Header from '../components/Header.astro';

const anim = {
    old: {
        name: "slide",
        duration: "0.5s",
        easing: "ease-in-out",
        direction: "right",
    },
    new: {
        name: "slide",
        duration: "0.5s",
        easing: "ease-in-out",
        direction: "reverse",},
};

const customTransition = {
    forwards: anim,
    backwards: anim,
}

const animLeft = {
    old: {
        name: "slideToLeft",
        duration: "0.5s",
        easing: "ease-in-out",
        direction: "left",
    },
    new: {
        name: "slideToLeft",
        duration: "0.5s",
        easing: "ease-in-out",
        direction: "reverse",
    },
};

const slideLeft = {
    forwards: animLeft,
    backwards: animLeft,
};

---
<BaseLayout title="Home">
    <main class="bg-black-1 w-full h-screen md:overflow-clip relative">
        
        <section id="Hero" transition:name="Hero" class="mx-auto w-full md:w-[1280px] h-screen flex flex-col md:flex-row justify-evenly items-center px-10">
            <div class="w-45 h-45 md:w-75 md:h-75 text-left rounded-full bg-black-1 me-shadow overflow-hidden" >
                <img id="me" src="/me.png" class="w-full" />
            </div>

            <div class="flex flex-col text-white-1 me-text">
                <strong class="flex flex-col text-4xl md:text-6xl font-rubik-one me-title">
                I'm Manuel Rubio
                <small class="text-xl md:text-2xl me-subtitle">
                    An alleged <span class="text-yellow-1">Software Engineer</span>
                </small>
                </strong>
                <p class="mt-5 text-md md:text-lg me-desc">
                Maybe the real Fibonacci Heap is the friends we make along the way...
                </p>
            </div>
        </section>
        
    </main>
</BaseLayout>

<script>
    document.addEventListener('astro:page-load', () => {
    });
    
    document.addEventListener('astro:before-preparation', () => {
        console.log('Preparing to load page...');
    });
</script>

<style is:inline>
    
    .me-shadow {
        box-shadow: 0px 0px 10em rgba(236, 204, 181, 0.25);
    }
</style>